# Інструкція для деплою клієнта на Vercel

## 📋 Передумови

1. Репозиторій клієнта має бути на GitHub
2. Всі зміни закомічені та запушені в репозиторій
3. У вас є аккаунт на Vercel

## 🚀 Крок 1: Підготовка репозиторію

### 1.1. Переконайтеся, що структура правильна:

```
client/
├── vercel.json          ✅
├── package.json         ✅
├── vite.config.ts       ✅
├── .gitignore          ✅
├── .env.example        ✅
├── src/
└── public/
```

### 1.2. Закомітьте та запуште зміни:

```bash
git add .
git commit -m "Prepare client for separate repository deployment"
git push origin main
```

## 🚀 Крок 2: Деплой на Vercel

### 2.1. Створіть новий проект на Vercel

1. Перейдіть на https://vercel.com
2. Увійдіть через GitHub
3. Натисніть **"Add New Project"** або **"Import Project"**
4. Виберіть ваш репозиторій з клієнтом

### 2.2. Налаштування проекту

**Root Directory:** 
- Залиште **ПОРОЖНІМ** (не вказуйте `./` або `client`)

**Framework Preset:**
- Виберіть **"Vite"** (Vercel має автоматично визначити)

**Build Command:**
- Залиште **ПОРОЖНІМ** (використається з `vercel.json`)

**Output Directory:**
- Залиште **ПОРОЖНІМ** (використається `dist` з `vercel.json`)

**Install Command:**
- Залиште **ПОРОЖНІМ** (використається `npm install`)

### 2.3. Environment Variables

Додайте змінну:

```
VITE_API_URL=https://your-server-app.vercel.app
```

**⚠️ ВАЖЛИВО:** 
- `VITE_API_URL` - це URL вашого серверного додатку
- Якщо сервер ще не задеплоєний, додайте цю змінну після деплою сервера
- Поки що можете залишити порожнім або вказати тимчасовий URL

### 2.4. Deploy

1. Натисніть **"Deploy"**
2. Дочекайтеся завершення build
3. Скопіюйте URL вашого деплою (наприклад: `https://your-client-app.vercel.app`)

## ✅ Крок 3: Перевірка

Після успішного деплою перевірте:

1. **Головна сторінка:** `https://your-client-app.vercel.app`
2. **Статичні файли:** перевірте, що CSS/JS завантажуються
3. **Роутинг:** перевірте, що SPA роутинг працює (наприклад, `/login`, `/register`)

## 🔄 Крок 4: Оновлення після деплою сервера

Після того, як задеплоїте сервер:

1. Перейдіть в Vercel Dashboard → Ваш проект → Settings → Environment Variables
2. Оновіть `VITE_API_URL` на URL вашого серверного додатку
3. Натисніть **"Redeploy"** або дочекайтеся автоматичного redeploy

## 📝 Примітки

- ✅ Статичні файли (CSS, JS, images) будуть правильно кешуватись
- ✅ SPA роутинг працює через `rewrites` в `vercel.json`
- ✅ Build автоматично запускається при кожному push в `main` branch
- ⚠️ Не забудьте оновити `VITE_API_URL` після деплою сервера!

## 🎯 Готово!

Ваш клієнт задеплоєний і готовий до використання!
