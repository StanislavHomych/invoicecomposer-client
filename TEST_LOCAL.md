# Тестування локально перед деплоєм

## 🧪 Тестування production build локально

### 1. Збірка проекту

```bash
cd client
npm run build
```

### 2. Запуск preview сервера

```bash
npm run preview
```

Додаток буде доступний на `http://localhost:4173`

### 3. Перевірка стилів

Відкрийте браузер і перевірте:

- ✅ Темний фон застосовується (`#1f1f1f`)
- ✅ Білий текст відображається правильно
- ✅ Шрифт Poppins завантажується
- ✅ Всі компоненти мають правильні стилі
- ✅ Немає FOUC (Flash of Unstyled Content)

### 4. Перевірка на різних сторінках

- `/` - головна сторінка (InvoicePublicPage)
- `/login` - сторінка входу
- `/register` - сторінка реєстрації
- `/dashboard` - дашборд (після входу)

### 5. Перевірка в різних браузерах

- Chrome/Edge
- Firefox
- Safari

## 🔍 Що перевіряти

1. **Стилі застосовуються одразу** - немає білого фону перед завантаженням
2. **Шрифти завантажуються** - текст відображається з правильним шрифтом
3. **Кольори правильні** - темний фон, білий текст
4. **Responsive дизайн** - працює на мобільних пристроях
5. **Немає помилок в консолі** - відкрийте DevTools

## 🐛 Якщо щось не працює

1. Перевірте консоль браузера на помилки
2. Перевірте Network tab - чи завантажуються всі файли
3. Перевірте, чи правильно працює `GlobalStyle` компонент
4. Перевірте, чи немає конфліктів CSS

## ✅ Після успішного тестування

```bash
git add .
git commit -m "Fix styles for production build"
git push origin main
```

Потім задеплойте на Vercel!
