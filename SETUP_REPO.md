# Налаштування окремого репозиторію для клієнта

## 📋 Крок 1: Ініціалізація Git репозиторію

Якщо клієнт ще не є окремим Git репозиторієм:

```bash
cd client
git init
git add .
git commit -m "Initial commit: Invoice Composer Client"
```

## 📋 Крок 2: Створення репозиторію на GitHub

1. Перейдіть на https://github.com
2. Натисніть **"New repository"**
3. Введіть назву (наприклад: `invoicecomposer-client`)
4. **НЕ** додавайте README, .gitignore або license (вони вже є)
5. Натисніть **"Create repository"**

## 📋 Крок 3: Підключення до GitHub

```bash
# Додайте remote репозиторій (замініть на ваш URL)
git remote add origin https://github.com/YOUR_USERNAME/invoicecomposer-client.git

# Або через SSH
git remote add origin git@github.com:YOUR_USERNAME/invoicecomposer-client.git

# Перевірте remote
git remote -v
```

## 📋 Крок 4: Push коду

```bash
# Перейменуйте гілку на main (якщо потрібно)
git branch -M main

# Запуште код
git push -u origin main
```

## 📋 Крок 5: Деплой на Vercel

Детальні інструкції дивіться в [DEPLOY.md](./DEPLOY.md)

## ✅ Готово!

Тепер ваш клієнт є окремим репозиторієм і готовий до деплою!
